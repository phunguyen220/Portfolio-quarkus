apiVersion: apps/v1
kind: Deployment
metadata:
  name: profile-service-quarkus
  namespace: portfolio
spec:
  replicas: 1
  selector:
    matchLabels:
      app: profile-service-quarkus
  template:
    metadata:
      labels:
        app: profile-service-quarkus
    spec:
      containers:
        - name: profile-service-quarkus
          image: portfolio-profile-service-quarkus:1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8085
          env:
            - name: QUARKUS_HTTP_HOST
              value: "0.0.0.0"

            - name: QUARKUS_HTTP_PORT
              value: "8085"

            - name: QUARKUS_MONGODB_CONNECTION_STRING
              valueFrom:
                configMapKeyRef:
                  name: portfolio-config
                  key: MONGODB_URI

            - name: QUARKUS_MONGODB_DATABASE
              value: "microservice-portfolio"
---
apiVersion: v1
kind: Service
metadata:
  name: profile-service-quarkus
  namespace: portfolio
spec:
  type: NodePort
  selector:
    app: profile-service-quarkus
  ports:
    - port: 8085
      targetPort: 8085
      nodePort: 30895