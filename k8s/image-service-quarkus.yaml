apiVersion: apps/v1
kind: Deployment
metadata:
  name: image-service-quarkus
  namespace: portfolio
spec:
  replicas: 1
  selector:
    matchLabels:
      app: image-service-quarkus
  template:
    metadata:
      labels:
        app: image-service-quarkus
    spec:
      containers:
        - name: image-service-quarkus
          image: portfolio-image-service-quarkus:1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8083
          env:
            - name: QUARKUS_HTTP_HOST
              value: "0.0.0.0"

            - name: QUARKUS_HTTP_PORT
              value: "8083"

            - name: QUARKUS_DATASOURCE_JDBC_URL
              valueFrom:
                configMapKeyRef:
                  name: portfolio-config
                  key: IMAGE_SERVICE_DB_URL

            - name: QUARKUS_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: portfolio-secret
                  key: MYSQL_USERNAME

            - name: QUARKUS_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: portfolio-secret
                  key: MYSQL_PASSWORD
---
apiVersion: v1
kind: Service
metadata:
  name: image-service-quarkus
  namespace: portfolio
spec:
  type: NodePort
  selector:
    app: image-service-quarkus
  ports:
    - port: 8083
      targetPort: 8083
      nodePort: 30893